interface:
  display_name: "Coffee Order Issue Loop"
  short_description: "Issue 단위 구현과 검증 품질 루프를 적용합니다"
  default_prompt: "Use $coffee-order-issue-loop as a coordinator-only workflow: assign implementation to Dev, review to Review, independent verification to QA, and evidence to Docs without Main editing or reviewing code."

policy:
  allow_implicit_invocation: true
