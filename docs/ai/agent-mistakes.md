# 에이전트 실수 기록

AI가 잘못 제안한 내용, 잘못된 이유, 수정 방식을 기록합니다.

| 날짜 | 실수 | 수정 |
| --- | --- | --- |
| 2026-07-10 | PR #31을 생성할 때 `Execution mode: STRICT`와 `Execution mode reason`을 본문에 넣지 않아 quality-gates가 실패했습니다. 재현 조건은 누락된 본문으로 생성된 기존 `pull_request` event를 rerun하는 경우입니다. `gh run view 29086275802 --repo namdongyeob/coffee-order-system --log-failed`로 해당 run의 두 필드 누락과 종료 코드 1을 확인했습니다. 이후 PR 본문만 수정하고 기존 run을 rerun했지만, rerun은 최초 PR event payload snapshot을 재사용해 같은 실패가 반복됐습니다. | PR 생성 전 harness가 요구하는 PR body 필수 필드를 사전 점검합니다. PR 본문을 수정한 뒤에는 기존 run을 rerun하지 않고, 현재 PR 본문을 반영하는 새 `pull_request` event를 생성한 후 quality-gates 결과를 확인합니다. |
| 2026-07-11 | Issue #29의 STRICT 흐름에서 필수 Review·QA 후속 역할이 남아 있고 Agent 완료 보고도 있었지만, Main이 live 또는 pending Agent가 있는 상태에서 final을 보내고 다음 역할을 즉시 배정하지 않아 부모 turn이 비활성화됐습니다. 그 결과 Codex Desktop의 active work timer가 멈추고 subagent 목록과 include-message 안내가 보이지 않아 사용자는 작업이 끝난 것처럼 보였으며 필수 Review·QA 후속 처리가 지연됐습니다. 이 순서에서 CPU나 모델 병목은 확인되지 않았고, 모델 전환은 새 active turn을 시작했을 뿐 원인을 해결했다는 근거가 없습니다. | Main은 final 전에 `list_agents`로 상태를 확인합니다. live Agent 또는 필수 다음 역할이 남아 있으면 commentary 상태를 유지하며 기다리거나 즉시 배정하고, 완료 mailbox를 받으면 곧바로 다음 역할을 시작합니다. 작업 중 상태 질문은 commentary로 답하고, final은 PR-ready, `BLOCKED`, 사용자 입력 필수 중 하나의 terminal 상태에서만 보냅니다. |
| 2026-07-11 | PR #34에서 stale `pr-body.md` snapshot을 삭제하고 GitHub PR 본문을 정본으로 유지하기로 합의했지만, PR #38에서 snapshot을 다시 생성했습니다. PR #38 리뷰 시 snapshot의 `CI pending`과 `remote branch 없음`이 실제 GitHub 본문의 CI PASS·remote branch 존재 상태와 달라 두 불일치가 재발한 것을 확인했습니다. | Docs Agent 절차에서 저장소 내부 `pr-body.md` snapshot을 생성하지 않습니다. PR 본문은 GitHub의 실제 본문만 갱신하고, harness 검사가 필요하면 workflow처럼 임시 파일을 사용합니다. |
| 2026-07-11 | PR #41 생성 전에 실제 PR body preflight를 생략해 `Execution mode` 필드를 bullet로 작성했고 최초 quality-gates가 실패했습니다. 이미 PR #31에서 확인한 stale `pull_request` payload 문제인데도 run `29138454684`를 한 번 rerun해 같은 최초 본문으로 다시 실패한 뒤 synchronize event를 만들었습니다. 본문 수정 과정의 인코딩 복구와 새 event 생성을 위해 빈 commit 두 개가 추가된 것도 이 복구 비용이며 선호 경로가 아닙니다. | PR 생성·수정 전 body를 저장소 밖 임시 파일에 만들고 `harness_gate.py --pr-body-file`로 검사한 뒤 그 파일을 `gh pr create/edit --body-file`에 사용합니다. 본문 수정 후 기존 run을 rerun하지 않고 새 edited event를 확인하며, workflow가 edited event를 받지 않으면 의미 있는 commit 하나의 synchronize event만 사용합니다. 빈 commit은 정상 절차가 아니라 복구 비용으로만 기록합니다. |
| 2026-07-12 | PR #43 initial 본문이 exact `Execution mode: STRICT` 대신 `STRICT mode:`를 사용해 quality-gates run `29168649292`가 실패했습니다. live PR 본문을 고쳐도 새 `pull_request` payload가 생기지 않았고, 기존 run rerun은 수정 전 body를 유지해 같은 실패를 반복했습니다. | PR 생성 전에 exact literal `Execution mode: STRICT`와 `Execution mode reason:`을 검사합니다. 생성 후 본문을 고쳤다면 현재 body를 담은 synchronize event가 필요합니다. 이를 자동화하는 wrapper나 edited-event workflow 변경은 별도 policy/workflow Issue이며 Issue #10에서는 구현하지 않습니다. |
| 2026-07-20 | #134 작업에서 장시간 wait 뒤 `team_orchestration` state를 기록·소비하지 않았고, non-ASCII worktree에서 Gradle을 시도했으며 commit·PR 없이 종료한 운영 실패가 재현되었습니다. | spawn 전 실제 absolute worktree·영향도·deadline·Java 선언을 register하고, wait 알림 뒤 heartbeat, timeout 뒤 lifecycle과 snapshot-once를 state에 기록합니다. non-ASCII Java worktree는 실행 전에 차단하며, 완료 전 commit·push·draft PR/evidence 상태를 명시적으로 확인합니다. |

## 기록 조건

재현 가능한 실제 하네스 실패와 evidence로 확인된 반복 workflow·오케스트레이션 절차 실패만 이 표에 기록합니다. 각 항목에는 실제 Issue 또는 PR 번호, 재현 조건 또는 확인 evidence, 관찰된 영향과 원인, 재발 방지 조치를 포함합니다. 추측한 실패, 한 번의 일시적 환경 오류, 아직 재현하지 못한 우려는 Attempt log에만 남기고 이 표에 중복 등록하지 않습니다.
