# ADR-002 Redisson과 DB 비관적 락

## 상태

Accepted. 튜터 확인은 여전히 권장합니다.

## 결정

Redisson은 주문/결제 진입 제어로 사용하고, DB 비관적 락은 포인트 최종 정합성 보장 장치로 사용합니다.

## 이유

Redisson은 다수 인스턴스 환경에서 같은 사용자 요청이 동시에 핵심 로직으로 들어오는 것을 줄입니다. DB 비관적 락은 Redis 장애나 락 만료 같은 상황에서도 최종 잔액 정합성을 보장합니다.